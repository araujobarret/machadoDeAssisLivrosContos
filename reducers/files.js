const initialState = {
  files: [],
  file: null,
  search: '',
};

const files = (state = initialState, action) => {
  switch(action){
    default:
      return state;
    break;
  }
}

export default files;
